# --- PersonaGen 프로젝트 환경 변수 설정 ---
# 이 파일은 프로젝트 실행에 필요한 환경 변수의 종류를 알려주는 '양식'입니다.
# 실제 비밀 키 값은 절대로 이 파일에 저장하지 마세요.
# 이 파일을 복사하여 .env 파일을 새로 만들고, 그 안에 실제 값을 입력하여 사용하세요.
# .env 파일은 .gitignore에 등록되어 있어 Git에 올라가지 않습니다.


# --- LLM & RAG 설정 ---

# OpenAI API 키 (페르소나 생성, 자기반성 등에 사용)
# (예: sk-aBcDeFgHiJkLmNoPqRsTuVwXyZ...)
OPENAI_API_KEY=

# (선택사항) '동적 RAG' 기능을 위한 웹 검색 API 키
# (예: Google Custom Search API, Tavily API 등)
SEARCH_API_KEY=


# --- 데이터베이스 설정 ---

# (선택사항) 향후 PostgreSQL 등 외부 데이터베이스 사용을 위한 연결 정보
# 현재는 로컬 SQLite를 사용하므로 비워두어도 됩니다.
# (예: postgresql://user:password@host:port/dbname)
DATABASE_URL=


# --- 외부 서비스 API ---

# (선택사항) 해외 복지 서비스 정보를 가져올 외부 API 키
# 실제 API를 찾기 전까지는 비워둡니다.
OVERSEAS_WELFARE_API_KEY=

# --- AWS 설정 (Bedrock 챗봇용) ---

# AWS 액세스 키 (Bedrock Claude 모델 사용)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-west-2

# PostgreSQL 데이터베이스 (복지 서비스 검색)
DB_HOST=seoul-ht-11.cpk0oamsu0g6.us-west-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=yeardream11!!